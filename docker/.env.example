# PostgreSQL — default password matches hydeclaw.toml [database].url
# Change both here and in config/hydeclaw.toml if you need a custom password.
POSTGRES_USER=hydeclaw
POSTGRES_PASSWORD=hydeclaw

