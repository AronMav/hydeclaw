# Tools Reference

> Tool descriptions are available via tool schemas (automatic) and `skill_use(action="list")` for skills.
> Use `tool_list(status="verified")` to see all YAML tools with endpoints.
> For detailed guides on creating tools/providers/channels — use `skill_use(action="load", name="...")`.
