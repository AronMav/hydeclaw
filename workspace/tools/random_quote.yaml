name: random_quote
description: "Get a random inspirational quote. Returns the quote text and author name."
endpoint: "https://api.forismatic.com/api/1.0/"
method: GET
parameters:
  method:
    type: string
    location: query
    default: "getQuote"
    description: "API method"
  format:
    type: string
    location: query
    default: "json"
    description: "Response format"
  lang:
    type: string
    location: query
    default: "ru"
    description: "Quote language (ru/en)"
auth:
  type: none
status: verified
