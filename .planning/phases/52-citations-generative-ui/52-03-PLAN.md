---
phase: 52-citations-generative-ui
plan: 03
type: execute
wave: 2
depends_on: [52-01, 52-02]
files_modified: []
autonomous: false
requirements: [CITE-01, GENUI-01, GENUI-02]

must_haves:
  truths:
    - "Footnote tooltips appear on hover in a real browser"
    - "Table and metric rich cards render correctly through the registry"
    - "Unknown card types show JSON fallback"
    - "A broken card component shows error fallback, not crash"
  artifacts: []
  key_links: []
---

<objective>
Human verification of citations and generative UI features in a running browser.

Purpose: Confirm visual and interactive behavior cannot be validated by automated tests alone.
Output: Verification that all success criteria pass in a real environment
</objective>

<execution_context>
@D:/GIT/bogdan/hydeclaw/.claude/get-shit-done/workflows/execute-plan.md
@D:/GIT/bogdan/hydeclaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-citations-generative-ui/52-01-SUMMARY.md
@.planning/phases/52-citations-generative-ui/52-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of citations and generative UI</name>
  <files></files>
  <action>
    Human verifies all features in a running browser. Start dev server with `cd ui && npm run dev`.

    **Citation Tooltips (CITE-01):**
    1. Send a message that causes the agent to use footnote citations
    2. Hover over footnote superscripts [1], [2] etc.
    3. Verify tooltip appears with cited source text -- no page navigation
    4. Verify footnote definitions section at bottom is NOT visible

    **Generative UI - Registry (GENUI-01):**
    5. Ask agent to use rich_card tool for a table
    6. Verify table renders as styled card (same as before refactor)
    7. Ask for a metric card
    8. Verify metric renders with value, trend icon, label

    **Error Boundary (GENUI-01):**
    9. Check browser DevTools: no React errors from card rendering

    **Unknown Card Type (GENUI-02):**
    10. If possible, trigger unknown card type -- verify JSON fallback renders
  </action>
  <verify>Human visual confirmation</verify>
  <done>All 4 success criteria confirmed visually in the browser</done>
</task>

</tasks>

<verification>
Human confirms all 4 success criteria pass visually in the browser.
</verification>

<success_criteria>
1. Footnote tooltips work on hover
2. Table and metric cards render correctly through CARD_REGISTRY
3. ErrorBoundary catches broken cards without crashing chat
4. Unknown card types show JSON fallback
</success_criteria>

<output>
After completion, create `.planning/phases/52-citations-generative-ui/52-03-SUMMARY.md`
</output>
