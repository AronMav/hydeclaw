---
phase: 48-optimistic-responsive-ui
plan: 03
type: execute
wave: 2
depends_on: [48-01, 48-02]
files_modified: []
autonomous: false
requirements: [OPTI-01, OPTI-02, OPTI-03, OPTI-04]

must_haves:
  truths:
    - "Thinking indicator starts before any SSE event when user sends a message"
    - "Agent switch shows skeleton when no cached data exists"
    - "Stream completion causes no visual flicker or blank frame"
    - "Message list DOM nodes are reused when transitioning from live to history view"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all OPTI requirements in the browser.

Purpose: Confirm that the optimistic UI changes produce the intended user experience — instant feedback, no flicker, smooth transitions.

Output: Verified OPTI-01 through OPTI-04 in browser.
</objective>

<execution_context>
@D:/GIT/bogdan/hydeclaw/.claude/get-shit-done/workflows/execute-plan.md
@D:/GIT/bogdan/hydeclaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-optimistic-responsive-ui/48-CONTEXT.md
@.planning/phases/48-optimistic-responsive-ui/48-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all OPTI requirements in browser</name>
  <files></files>
  <action>
    Human verifies all four OPTI requirements in browser:

    1. Start dev server: `cd ui && npm run dev`
    2. Open http://localhost:3000/chat

    **OPTI-01 — Instant thinking indicator:**
    3. Select an agent and type a message
    4. Click Send — verify the pulsing dots thinking indicator appears IMMEDIATELY (no delay waiting for server)
    5. The indicator should show the correct agent name above the dots
    6. When assistant text starts arriving, the thinking indicator should disappear and be replaced by the response

    **OPTI-02 — Agent-switch skeleton:**
    7. While on agent A with a conversation, switch to agent B (one you haven't talked to yet or clear cache)
    8. Verify a skeleton preview appears (3 items with avatar circle + text lines) while history loads
    9. Switch back to agent A — if cache exists, messages should appear instantly (no skeleton)

    **OPTI-03 — Flicker-free transition:**
    10. Send a message and wait for the stream to complete
    11. Watch carefully — after the stream finishes, there should be NO visible flicker, blank frame, or content jump
    12. Open Chrome DevTools Performance tab, record during a stream completion, verify no layout shift

    **OPTI-04 — Reference stability:**
    13. Open Chrome DevTools Elements panel
    14. Find an assistant message DOM node during streaming, note its element reference
    15. After stream completes and live-to-history transition occurs, verify the assistant message DOM node was NOT destroyed and recreated (same element reference)
  </action>
  <verify>Human confirms all 4 OPTI requirements pass visual inspection</verify>
  <done>User types "approved" confirming all OPTI behaviors work correctly in browser</done>
</task>

</tasks>

<verification>
All four OPTI requirements verified by human observation in browser.
</verification>

<success_criteria>
- User confirms thinking indicator appears instantly on Send
- User confirms skeleton appears on agent switch with no cache
- User confirms no flicker during live-to-history transition
- User confirms DOM node stability for assistant messages
</success_criteria>

<output>
After completion, create `.planning/phases/48-optimistic-responsive-ui/48-03-SUMMARY.md`
</output>
