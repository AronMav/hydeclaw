diff --git a/node_modules/@assistant-ui/store/dist/tapClientLookup.js b/node_modules/@assistant-ui/store/dist/tapClientLookup.js
index da6d6ad..c23b160 100644
--- a/node_modules/@assistant-ui/store/dist/tapClientLookup.js
+++ b/node_modules/@assistant-ui/store/dist/tapClientLookup.js
@@ -26,14 +26,17 @@ export function tapClientLookup(getElements, getElementsDeps) {
         state,
         get: (lookup) => {
             if ("index" in lookup) {
-                if (lookup.index < 0 || lookup.index >= keys.length) {
-                    throw new Error(`tapClientLookup: Index ${lookup.index} out of bounds (length: ${keys.length})`);
+                if (lookup.index < 0 || lookup.index >= resources.length) {
+                    // Stale index during reactive update — clamp to last valid resource
+                    const clamped = Math.max(0, resources.length - 1);
+                    return resources[clamped]?.methods;
                 }
                 return resources[lookup.index].methods;
             }
             const index = keyToIndex[lookup.key];
             if (index === undefined) {
-                throw new Error(`tapClientLookup: Key "${lookup.key}" not found`);
+                // Stale key during reactive update — return first resource as fallback
+                return resources[0]?.methods;
             }
             return resources[index].methods;
         },
diff --git a/node_modules/@assistant-ui/store/src/tapClientLookup.ts b/node_modules/@assistant-ui/store/src/tapClientLookup.ts
index b59048d..c38077e 100644
--- a/node_modules/@assistant-ui/store/src/tapClientLookup.ts
+++ b/node_modules/@assistant-ui/store/src/tapClientLookup.ts
@@ -61,17 +61,18 @@ export function tapClientLookup<TMethods extends ClientMethods>(
     state,
     get: (lookup: { index: number } | { key: string }) => {
       if ("index" in lookup) {
-        if (lookup.index < 0 || lookup.index >= keys.length) {
-          throw new Error(
-            `tapClientLookup: Index ${lookup.index} out of bounds (length: ${keys.length})`,
-          );
+        if (lookup.index < 0 || lookup.index >= resources.length) {
+          // Stale index during reactive update — clamp to last valid resource
+          const clamped = Math.max(0, resources.length - 1);
+          return resources[clamped]!.methods;
         }
         return resources[lookup.index]!.methods;
       }
 
       const index = keyToIndex[lookup.key];
       if (index === undefined) {
-        throw new Error(`tapClientLookup: Key "${lookup.key}" not found`);
+        // Stale key during reactive update — return first resource as fallback
+        return resources[0]!.methods;
       }
       return resources[index]!.methods;
     },
