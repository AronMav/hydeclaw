// Placeholder — embedding logic will be moved here from memory.rs in a later task.

pub trait EmbeddingService {}

pub struct ToolgateEmbedder;

pub fn fmt_vec(_v: &[f32]) -> String {
    String::new()
}
