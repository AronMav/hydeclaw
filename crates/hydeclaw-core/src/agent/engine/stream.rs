//! REF-01 Task 2 target — populated in Task 2 of plan 66-02.
//!
//! Will own: `handle` / `handle_isolated` / `handle_with_status` entry points,
//! `ProcessingPhase` wire enum, `ProcessingGuard` RAII tracker, and the
//! `StreamEvent` re-export for external callers.
