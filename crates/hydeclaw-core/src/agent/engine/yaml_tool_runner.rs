//! REF-01 Task 4 target — populated in Task 4 of plan 66-02.
//!
//! Will own: YAML-tool env/oauth resolver + per-engine caches —
//! `SecretsEnvResolver`, `make_resolver`, `make_oauth_context`,
//! `invalidate_yaml_tools_cache`, `check_search_cache`, `store_search_cache`,
//! `format_tool_error`, `truncate_preview`, and the `search_cache_key` helper.
