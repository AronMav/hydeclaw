//! REF-01 Task 3 target — populated in Task 3 of plan 66-02.
//!
//! Will own: `ApprovalResult` enum + `AgentEngine::needs_approval` +
//! `AgentEngine::resolve_approval` — the engine-side approval glue consumed
//! by `src/agent/approval_manager.rs` via `pub use` re-exports.
